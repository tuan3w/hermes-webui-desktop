{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "strip",
  "description": "Capabilities for the pin strip webview — core IPC only, no shell access",
  "local": true,
  "windows": ["strip"],
  "permissions": [
    "core:default"
  ]
}
